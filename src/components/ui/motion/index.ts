/**
 * Motion Components Barrel Export
 * @module components/ui/motion
 */

// Motion Configuration & Presets
export {
    springPresets,
    durationPresets,
    easingPresets,
    fadeVariants,
    scaleVariants,
    slideUpVariants,
    slideRightVariants,
    slideLeftVariants,
    popVariants,
    buttonHoverScale,
    buttonTapScale,
    cardHoverEffect,
    glowEffect,
    staggerItem,
    pageTransitionVariants,
    modalVariants,
    drawerVariants,
    backdropVariants,
} from './MotionConfig';

// Animated Container Components
export {
    AnimatedContainer,
    StaggerContainer,
    StaggerItem,
    FadeInWhenVisible,
} from './AnimatedContainer';

// Page Transitions
export {
    PageTransition,
    FadePage,
    RouteTransitionContainer,
} from './PageTransition';
